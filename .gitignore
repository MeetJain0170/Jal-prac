__pycache__/
*.pyc
data
# Epoch checkpoints too large; best_model.pth via Git LFS
outputs/checkpoints/checkpoint_epoch_*.pth
useless
outputs

# ── Ultralytics / YOLO artifacts ─────────────────────────────────────────────
# Training runs (weights, plots, results)
runs/

# Model weights (YOLO .pt files are large; keep them out of git)
*.pt

# Ultralytics caches / outputs
*.engine

*.torchscript
*.tflite
*.mlpackage

# Dataset caches created by Ultralytics
**/*.cache
*.pt